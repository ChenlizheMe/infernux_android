<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:required="true"
        android:version="1" />
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:required="true"
        android:version="4194304" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:appCategory="game"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/infernux_launcher"
        android:isGame="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/infernux_launcher"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="infernux.resolution_scaling"
            android:value="@ANDROID_RESOLUTION_SCALING@" />
        <meta-data
            android:name="infernux.target_dpi"
            android:value="@ANDROID_TARGET_DPI@" />
        <activity
            android:name=".InfernuxActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="layoutDirection|locale|fontScale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:exported="true"
            android:launchMode="singleInstance"
            android:resizeableActivity="false"
            android:screenOrientation="@ANDROID_SCREEN_ORIENTATION@"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.GAME" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
